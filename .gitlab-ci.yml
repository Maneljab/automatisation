stages:
  - test
test:
  image: python:3.8
  before_script:
    - pip install -r requirements.txt
  script:
    - robot C:/projetauto/Tests/
  artifacts:
    when: always
    paths:
      - C:/projetauto/Resultats/output.xml
